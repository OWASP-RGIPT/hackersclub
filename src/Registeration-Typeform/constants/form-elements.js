import {
  Home,
  Name,
  BranchName,
  EmailAddress,
  PhoneNumber,
  File,
  Success,
} from '../components';

const componentArray = [
  Home,
  Name,
  BranchName,
  EmailAddress,
  File,
  PhoneNumber,
  Success,
];

const formElements = {
  componentArray,
  numberOfElements: componentArray.length,
};

export default formElements;
